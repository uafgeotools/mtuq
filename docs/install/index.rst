Installation
============

Virtual environments are necessary in Python to avoid dependency conflicts. Because conda doesn't fully support "editable" installations, virtualenv is currently the preferred environment manager.  This may change in future versions.

.. toctree::

   env_conda
   env_virtualenv
   env_custom

